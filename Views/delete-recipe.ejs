<h1 class="pb-4">Search Results</h1>

<div class="px-4 py-5 my-5 text-center">
  <% if(recipe != null) { %>

    <h1 class="display-5 fw-bold">Deleting Your Recipe: <%= recipe.name %>!</h1>
    <div class="col-lg-6 mx-auto">
      <p class="lead">Sorry to see you go!</p>
    </div>
    <div class="col-12 col-md-4">
      <img src="/Uploads/<%- recipe.image %>" class="img-fluid sticky-top" style="top: 20px;" alt="<%- recipe.name %>" Loading="lazy">
    </div>
  <% } else{ %>
    <p>No Recipe was found to delete!</p>
  <% } %>
</div>